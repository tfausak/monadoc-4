version: v1.0
name: Deploy pipeline

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Deploy block
    task:
      secrets:
        - name: aws-credentials
      jobs:
        - name: Deploy job
          commands_file: deploy.sh
